# Default: normalize line endings to LF on commit
* text=auto eol=lf

# Windows-only scripts: keep CRLF so cmd.exe / PowerShell parse them safely
*.cmd text eol=crlf
*.bat text eol=crlf
*.ps1 text eol=crlf

# Shell scripts always LF (executed on macOS/Linux)
*.sh text eol=lf
